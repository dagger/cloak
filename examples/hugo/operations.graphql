query Generate($src: FSID!) {
  hugo {
    generate(src: $src) {
      id
    }
  }
}

query Deploy($contents: FSID!, $bucketURL: String!, $subdir: String, $token: SecretID!) {
  hugo {
    deploy(contents: $contents, bucketURL: $bucketURL, subdir: $subdir, token: $token) {
	    id
    }
  }
}
