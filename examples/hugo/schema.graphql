extend type Query {
  hugo: hugo!
}

type hugo {
  generate(src: FSID!, themeGitURL: String!, hugoVersion: String): Filesystem!
  deploy(contents: FSID!, bucketURL: String!, subdir: String, token: SecretID!): Filesystem!
}
