{
  git {
    remote(url: "github.com/dagger/dagger") {
      pull(ref: "main") {
        dockerbuild(dockerfile: "Dockerfile") {
          exec(input: { args: ["dagger", "version"] }) {
            stdout
          }
        }
      }
    }
  }
}
